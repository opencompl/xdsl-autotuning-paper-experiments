[project]
name = "autotuner"
version = "0.1.0"
description = "Benchmarks repository for xDSL autotuning paper."
readme = "README.md"
requires-python = ">=3.11.2"
dependencies = [
    "jinja2>=3.1.6",
    "matplotlib>=3.10.3",
    "pandas>=2.3.1",
    "snakemake>=8.30.0",
    "xdsl[dev]",
]

[project.entry-points.'xdsl.universe']
autotuner = 'autotuner.universe:AUTOTUNER_UNIVERSE'

[tool.uv.sources]
xdsl = { git = "https://github.com/xdslproject/xdsl.git", rev = "63f08ec6c21b034faf6777fdb9486de2cfdbf0ef" }

[project.scripts]
hello = "autotuner.main:main"
plot-ttile = "autotuner.plot_ttile:main"
plot-cube = "autotuner.plot_cube:main"
libxsmm-gemm = "autotuner.libxsmm_gemm.libxsmm_generator_gemm_driver:main"

[tool.setuptools]
package-dir = { "autotuner" = "src/autotuner" }
packages = ["autotuner"]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pyright]
include = ["src", "tests"]
