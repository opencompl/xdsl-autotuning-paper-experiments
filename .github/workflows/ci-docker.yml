# For more information see: https://help.github.com/en/actions/language-and-framework-guides/using-python-with-github-actions

name: CI - Docker Tests

on:
  # Trigger the workflow on push or pull request,
  # but only for the master branch
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    # Pinned toolchain image (rebuild/push via publish-docker workflow on version tags).
    # Bump this when the Dockerfile changes and a new image is published.
    container: ghcr.io/opencompl/xdsl-autotuning-ci:0.19.0
    env:
      UV_LINK_MODE: copy
      # Job containers often replace PATH; uv lives in /bin, git in /usr/bin (xdsl is a
      # git dependency), and the project venv is on PATH for make tests.
      PATH: /opt/venv/bin:/bin:/usr/local/bin:/usr/bin:/sbin:/bin

    steps:
      - uses: actions/checkout@v4

      # TODO: remove once the Docker image ships mlir-opt on PATH directly
      - name: Symlink mlir-opt-20 to mlir-opt
        run: ln -sf /usr/local/bin/mlir-opt-20 /usr/local/bin/mlir-opt

      # GitHub Actions overrides the image ENTRYPOINT, so we must sync here (same as
      # docker/entrypoint.sh) before running tests.
      - name: Sync project into image venv
        working-directory: ${{ github.workspace }}
        run: uv sync --locked --inexact

      - name: Run tests target
        working-directory: ${{ github.workspace }}
        run: make tests

      - name: Prune uv cache
        working-directory: ${{ github.workspace }}
        run: uv cache prune --ci
